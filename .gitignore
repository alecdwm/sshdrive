# Xcode
*.xcodeproj/
*.xcworkspace/
xcuserdata/
DerivedData/
build/
*.xcuserstate
*.xcscmblueprint

# SwiftPM
.build/
.swiftpm/
Package.resolved

# Signing and local config
*.mobileprovision
*.provisionprofile
*.p12
Local.xcconfig

# macOS
.DS_Store

# The helper crate builds here (DESIGN.md section 10.1 ships them from CI)
helper/target/
# A locally built helper embedded into the bundle by scripts/mac-build.sh
Resources/helper/

# The Homebrew tap is its own repository (alecdwm/homebrew-tap), checked out here for convenience.
packaging/homebrew-tap/
